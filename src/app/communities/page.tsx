import { Typography } from '@mui/material'
import { type Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Communities',
}

export default function Page() {
  return <Typography variant="h2">Communities Page</Typography>
}
