import { type ReactElement } from 'react'
import { Icon, type IconProps } from '@mui/material'

const IconSelect = (props: IconProps): ReactElement => {
  return <Icon {...props}>keyboard_arrow_down</Icon>
}

export default IconSelect
